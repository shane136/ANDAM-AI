# Server-only Gemini credential used by api/chat.ts.
# Configure this for Development, Preview, and Production in Vercel.
# Never rename it to VITE_GEMINI_API_KEY; VITE_ variables are exposed in browsers.
GEMINI_API_KEY="your_gemini_api_key"
